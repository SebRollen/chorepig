<h2 class="mt-6 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900">Account</h2>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: "space-y-6", method: :put}) do |f| %>
  <div>
    <%= f.label :email, class: "block text-sm font-medium leading-6 text-gray-90" %>
    <div class="mt-2">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-pink-600 sm:text-sm sm:leading-6" %>
    </div>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div class="text-xs text-gray-600">Currently waiting on confirmation for change to: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div>
    <%= f.label :password, class: "block text-sm font-medium leading-6 text-gray-90" %>
    <div class="mt-2">
      <%= f.password_field :password, autocomplete: "new-password", class: "block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-pink-600 sm:text-sm sm:leading-6" %>
    </div>
  </div>

  <div>
    <%= f.label :password_confirmation, class: "block text-sm font-medium leading-6 text-gray-90" %>
    <div class="mt-2">
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-pink-600 sm:text-sm sm:leading-6" %>
    </div>
  </div>

  <div>
    <%= f.label :current_password, class: "block text-sm font-medium leading-6 text-gray-90" do %>
      Current password <span class="font-normal text-xs">(needed to save)</span>
    <% end %>

    <div class="mt-2">
      <%= f.password_field :current_password, autocomplete: "current-password", class: "block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-pink-600 sm:text-sm sm:leading-6" %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit "Save", class: "flex w-full justify-center rounded-md bg-pink-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-pink-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-pink-600", data: { turbo: true } %>
  </div>

  <h3 class="mt-6 text-lg font-bold leading-9 tracking-tight text-gray-900">Delete my account</h3>

  <div class="text-sm text-gray-700">
    <p>Your data is exactly that, yours. You can delete your account whenever you want, but some things to know first:</p>
    <ol class="mt-1 list-decimal">
      <li>Deleting your account will delete all your task data on our servers, but the data will still exist on your local device.</li>
      <li>We will no longer sync your data between devices.</li>
      <li>Since you created your account using a perpetual license, you will not be able to reactivate your account without buying another license automatically. If you do decide to come back, email us and we can get you set back up using the original license.</li>
    </ol>
  </div>

  <%= button_to "Delete", registration_path(resource_name),class: "rounded bg-red-600 px-2 py-1 text-sm font-semibold text-white shadow-sm hover:bg-red-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-600" , data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete %>
<% end %>
